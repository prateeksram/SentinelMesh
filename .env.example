# Copy to .env and fill in (never commit .env).
# Scene gen chain: GenieX (on-device) → OpenAI chat → template.

OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini
OPENAI_SCENE_MODEL=gpt-4o-mini
OPENAI_IMAGE_MODEL=gpt-image-1
